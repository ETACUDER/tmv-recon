# Status — 2026-04-28

Comprehensive snapshot after one day of building. Pairs with `TODO.md` for what unlocks once Urvashi's Tally backup arrives.

## Built today

### 1. Project scaffold + LLM keys
- `tmv-recon/` venv, pyproject, requirements
- Gemini 2.5 Pro + Flash + Claude Sonnet 4.6 — keys live, smoke tests pass
- `parsers/{pdf,excel}.py` — pypdf, pdfplumber, pandas

### 2. Excel ↔ Tally integration (`integration/`)
- `mapping.py` declarative ColumnMap (YAML or dict)
- `transforms.py` — date (ISO/Indian/`YYYYMMDD`/Excel-serial), amount (`₹`/`Rs`/commas/parens-negative)
- `pipeline.py` — two modes: row-per-voucher (bank stmt) and compound (group_by → multi-entry voucher)
- `validators.py` — mandatory fields + Dr/Cr balance + known-ledger check
- `presets/`: `bank_statement`, `sales_register`, `purchase_register`, `journal`, `ptm_payment`
- `cli.py` (`tmv-recon-import`) — Excel + preset → XML / CSV / live POST
- 4 presets + tolerant header normalizer (covers Agoda's `INVOCIE` typos)

### 3. Tally HTTP/XML connectors (`tally/`)
- `xml.py` — Import envelopes (Vouchers + Masters)
- `http.py` — POST to running Tally `:9000`
- `connectors.py` — pull-side: `list_companies`, `day_book`, `ledger_outstandings`, `bills_receivable/payable`, `trial_balance`, `voucher_register` → DataFrames
- `cli_pull.py` (`tmv-recon-pull`) — CLI wrapper
- `round_trip.py` — Mac → Create company / ledgers / Sales / Journal / Pull Day Book → verify narration round-trip

### 4. ETL pipeline (`etl/`)
- `bucket.py` — content-priority classifier; symlinks 105 source files (Excel/CSV/PDF) into `data/{booking,invoices,payments,tally}/{raw,processed}/`
- `models.py` — canonical Booking/Invoice/Payment/Match dataclasses
- `extract/booking.py` — Agoda; handles 12 header-typo variants, `(rate*nights)` annotation, comma-list invoice numbers, `ISSUED`-style header shift, TOTAL footer rows. Emits 3,476 rows × 20 batches.
- `extract/invoice.py` — EZ folio → invoice rollup. GST split (CGST/SGST). Emits 304 invoices, 300 reconcile cleanly.
- `extract/payment.py` — PTM raw 123-col primary; Indian Bank header-row search; UTR/RRN extraction from bank Description. Emits 316 PTM + 1,363 bank lines.
- `recon.py` — matcher with prioritised rules: PTM↔Bank by Paytm narration + amount + date (146/316 covered), PTM↔Invoice by amount + date + guest fuzzy, Booking↔Invoice by `invoice_no` exact + guest+arrival fallback.
- `tally_signal.py` — file-presence proxy (since no real Tally export): 1,994/5,459 rows already booked (36.5%); pending-by-month rollup.
- `_discovery_{booking,invoices,payments}.md` — schema-discovery reports from parallel agents.
- `_flow.md` — flow chart + ER diagram.
- `README.md` — iterative spec.

### 5. Web UI (`web/`)
- FastAPI + Tailwind/Alpine SPA at `tmv-recon-ui` → `http://127.0.0.1:8000/`
- Dashboard: canonical row counts, match counts, Tally entry status, pending-by-month
- Sidebar: file buckets with row counts
- Per-file: column preview + mapping highlighting + voucher preview + XML/CSV download + live "Push to Tally"
- Live Tally endpoint health check

### 6. Tally on Azure VM (`services/windows-test/`)
- TallyPrime 7.0 installed on `win-test-01` (Azure)
- HTTP/XML endpoint live on `http://20.219.50.8:9000/` (verified `STATUS=1`)
- NSG inbound TCP 9000 open from current IP
- Windows Firewall configured
- Scheduled task auto-launches Tally on shotrush login
- Bootstrap PowerShell scripts for company creation via SendKeys (interactive scheduled task)

## Verified working

- `Net + CGST + SGST = Gross` for 300/304 EZ invoices (4 are rounding cases)
- `INVOICE AMT = From Agoda + COMM+GST` exactly, max 2.3e-13 across 370-row sample (Agoda)
- `Settled = Amount − Commission − GST` exact across 261 successful PTM rows
- `GST = 18% × Commission` consistently on PTM
- 21 unit tests passing
- Tally HTTP endpoint reachable from Mac, accepts XML, returns parsed responses
- 105 files classified, 12 LIC PDFs quarantined (mistakenly bundled), 30 tally-bucket files identified as dupes (no real Tally export)

## Known data gaps

| Gap | Status |
|---|---|
| EZ sales export covers only Apr 1–28 2025 | needed: same period as PTM (Dec 25–Feb 26) |
| Real Tally Day Book / Voucher XML / company backup | absent — `tallyData/` is dupes of input Excels |
| GoMT data Nov 25+ (only single Apr–Oct CSV) | needed: monthly exports |
| PTM Rooftop / F&B raw 123-col Dec–Feb | only slim processed subsets available |
| Hotel main bank Dec 25+ statements | absent |
| TMV PMS receipt PDFs (with invoice number → bridge) | the 12 PDFs we received are LIC insurance, not hotel |
| AGODA.rar | not extracted |
| Loaded company on the Tally VM | needs 30-sec RDP creation OR backup restore |

## Open questions for Urvashi (need her call)

1. Why Journal vouchers for payments instead of Receipt vouchers?
2. Should `Sundry Debtors` split per OTA (Agoda / GoMT / Walk-in)?
3. **Commission + GST on aggregator settlements** — where in Tally? Current approach posts only gross (gap of ₹46k commission + ₹8k GST input credit unaccounted in our slice).
4. Rate-change credit notes — Credit Note voucher type or sales reversal?
5. F&B vs Hotel — same Tally company, separate ledger groups?
6. TCS/TDS GoMT — where in Tally?
7. **GST rate** — EZ data shows 12% (CGST 6 + SGST 6) on accommodation. Tally ledger label seen on screen: `SALE ACCOMODATION GST @ 5%`. Which is canonical?

## Match-rate caveat

The matcher has the right logic (verified end-to-end). The 0/316 PTM↔Invoice match rate is **not a bug** — it's because the EZ invoice export covers only April 2025 while payments cover Dec 2025–Feb 2026. **Zero temporal overlap.** Same explanation for the 40/3476 Booking↔Invoice rate. Get a same-period EZ export and re-run; numbers will move.
